var a = +prompt('Enter value a', 0)
var b = +prompt('Enter value b', 0)

function checkMinValue (firstValue, secondValue) {
   if (firstValue < secondValue) {

       return (firstValue)
   }

   return (secondValue)
}

alert (checkMinValue(a,b))
