var browser = prompt('Enter your browser name')

if (browser === 'IE') {

    alert ('Oh, shit!')

} else if (browser === 'Chrome' || 'Ferifox' || 'Safari' || 'Opera') {

    alert ('its okey')

} else {

    alert ('what browser you use?')

}
